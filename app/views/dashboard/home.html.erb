
<% if flash[:notice].present? %>
 <script> alert("<%= flash[:notice]%>"); </script>
<% end %>

<table border="1">
	<thead><td> Name </td>
          <td> Description </td>
          <td> links </td>
         <% if @role == 'Manager' %>
          <td> delete </td>
          <% end %>
	</thead>
	
	<tbody>
		<% @projects.each do |p| %>
		<tr>
		<td><%= p.name %></td>
		<td><%= p.description %> </td>
		<% url = "project_details/#{p.id}" %>
		 <% url2 = "/bugs/show/#{p.id}" %>
		<td> <a href=<%= url %>>Show Users</a>  
           <a href=<%= url2 %>>Show Bugs</a> 

		</td>
		<% if @role == 'Manager' %>
		<% url = "/projects/destroy/#{p.id}" %>
		<td><a href=<%= url %>>Delete</a> </td>
			<% end %>
		</tr>
		<% end %>
	
</tbody>

</table>

<% if @role == 'Manager' %>

<%= button_to 'Create new project', projects_new_path, method: :get %>

<% end %>

